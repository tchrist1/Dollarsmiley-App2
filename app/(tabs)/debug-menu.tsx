import { View } from 'react-native';

/**
 * Dummy screen for debug menu tab
 * This screen is never actually shown as the tab press is prevented
 * and the modal is opened instead
 */
export default function DebugMenuScreen() {
  return <View />;
}
